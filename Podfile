# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ArmyFitViewCode' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for ArmyFitViewCode
  pod 'KeychainSwift', '~> 20.0'
  pod 'Kingfisher', '~> 7.0'
  
  target 'ArmyFitViewCodeTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'ArmyFitViewCodeUITests' do
    # Pods for testing
  end
  
end
